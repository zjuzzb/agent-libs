#kubectl create --namespace=test-ns -f busybox-rc.yaml
#kubectl get rc --namespace=test-ns
#kubectl get pods --namespace=test-ns
#kubectl delete rc --namespace=test-ns busybox-1
kind: ReplicationController
apiVersion: v1
metadata:
  name: busybox-1
spec:
  replicas: 1
  # selector identifies the set of Pods that this
  # replication controller is responsible for managing
  selector:
    name: busybox-1
    role: busy
    app: demo
  template:
    spec:
      containers:
        - name: busybox-1
          image: ubuntu
          command: ["/bin/bash", "-c", "while true; do sleep 1; done"]
          ports:
          - containerPort: 8080
            name: busy-port
    metadata:
      labels:
        # Important: these labels need to match the selector above
        # The api server enforces this constraint.
        name: busybox-1
        role: busy
        app: demo
  labels:
    name: rc-busybox-1
    app: demo

