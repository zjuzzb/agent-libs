#kubectl create --namespace=test-ns -f service.yaml
#kubectl get services --namespace=test-ns
#kubectl delete services --namespace=test-ns svc-1
apiVersion: v1
kind: Service
metadata:
  labels:
    name: svc-1
  name: svc-1
spec:
  ports:
    - port: 80
      targetPort: busy-port
  selector:
     name: busybox-1
     role: busy

