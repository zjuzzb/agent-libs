if [ $1 = 0 ]; then # package is being erased
	# Any needed actions here on uninstalls
else
	# Upgrade
	/sbin/service dragent condrestart > /dev/null 2>&1
fi

dkms remove -m draios -v %{version} --all --rpm_safe_upgrade
