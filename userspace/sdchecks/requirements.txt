ntplib==0.3.3

# note: simplejson is used in many checks to inteface APIs
# This library bundles a .so dependency but does not depend
# on python version so we install it here
simplejson==3.16.0

# used by many appchecks
requests==2.22.0

# checks.d/docker.py
# docker-py pulls in requests
docker-py==1.10.6

# sdchecks.py
sysdig-tracers==0.1.2

# checks.d/network.py
psutil==5.6.2

pyopenssl==19.0.0
netifaces==0.10.9
posix_ipc==1.0.0

###########################################################
# These modules are for checks. But they are
# installable just fine anywhere. So we install
# them all. They're usually pure python and don't
# need any external deps.
###########################################################

# checks.d/riak.py
httplib2==0.9

# checks.d/kafka_consumer.py
kafka-python==1.3.4
kazoo==2.4.0

# checks.d/postgres.py
pg8000==1.10.2

# checks.d/gearman.py
gearman==2.0.2

# checks.d/mcache.py
python-memcached==1.53

# checks.d/redis.py
redis==2.10.5

# checks.d/hdfs.py
snakebite==2.7.1

# checks.d/snmp.py
# Require a compiler because pycrypto is a dep
pysnmp-mibs==0.1.6
pysnmp==4.4.6

# checks.d/riakcs.py
boto==2.36.0

# checks.d/prometheus.py
prometheus_client==0.0.21

# checks.d/mongo.py
pymongo==3.2

# checks.d/spark.py
beautifulsoup4==4.5.1

# solr/solr_5.py
enum34==1.1.6


