file(COPY . DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/ FILES_MATCHING PATTERN *.py)
add_custom_target(sdchecks ALL COMMAND python -m compileall .)
install(DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/ DESTINATION ${DRAIOS_BIN_PREFIX}/lib/python COMPONENT agent FILES_MATCHING PATTERN *.py*)
install(PROGRAMS sdchecks
	DESTINATION ${DRAIOS_BIN_PREFIX}/bin
	COMPONENT agent)